# LLM Training Studio — Python dependencies
# Install with:
#   pip install -r requirements.txt
#
# PyTorch (CUDA 12.8 — Blackwell/Ada/Ampere GPUs):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# llama-cpp-python (optional — GGUF inference without Ollama):
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu128

# ── Training Studio server ────────────────────────────
flask>=3.1
flask-cors>=6.0
psutil>=5.9

# ── LLM training (train.py) ──────────────────────────
transformers>=4.47
peft>=0.14
accelerate>=1.0
bitsandbytes>=0.43
datasets>=3.0
trl>=1.0
sentencepiece>=0.2
safetensors>=0.4

# ── Export / GGUF conversion (generate_llm.py) ───────
huggingface_hub>=0.24
gguf>=0.10

# ── Desktop UI (PyQt6 launcher) ──────────────────────
PyQt6>=6.7
PyQt6-WebEngine>=6.7

# ── Utilities ────────────────────────────────────────
requests>=2.31
tqdm>=4.65
